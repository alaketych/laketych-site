datasource db {
  provider = "sqlite"
  url      = "file:./blog.db"
}

generator client {
  provider = "prisma-client-js"
}

model Post {
  id Int @id @default(autoincrement())
  title String
  excerpt String
}